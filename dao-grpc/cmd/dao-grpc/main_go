package main

import (
	"log"

	"github.com/kelseyhightower/envconfig"
)

type Config struct {
	DB string `envconfig:"DB_TYPE"`
}

func main() {
	var cfg Config
	err := envconfig.Process("", &cfg)
	if err != nil {
		log.Fatal(err)
	}
}
